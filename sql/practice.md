# Structured Query Language

## SQL Syntax

Statements:  
  `CREATE DATABASE`,
  `CREATE TABLE`, 
  `CREATE VIEW`,  
  `DESCRIBE`,  
  `SELECT`,  
  `ALTER TABLE`,  
  `INSERT INTO`,  
  `DROP TABLE`,  
  `DELETE FROM`,  
  `UPDATE`,  
  `INNER/LEFT/RIGHT/FULL JOIN`,  

Functions:  
  ``,